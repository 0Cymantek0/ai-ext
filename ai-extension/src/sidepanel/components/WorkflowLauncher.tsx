/**
 * WorkflowLauncher stub — Task 3 will provide the full implementation.
 * This stub exists so the integration test file can resolve the import.
 */
import * as React from "react";

export interface WorkflowLauncherProps {
  [key: string]: any;
}

export function WorkflowLauncher(_props: WorkflowLauncherProps): React.ReactNode {
  return null;
}
