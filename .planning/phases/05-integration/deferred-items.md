- `pnpm run lint` currently fails outside this plan's scope due existing repo-wide issues, including parser configuration for `src/__tests__/contentCapture.capture.spec.ts` and widespread CRLF/prettier violations in untouched files such as `ai-extension/src/background/abbreviation-storage.ts` and `ai-extension/src/background/service-worker.ts`.
- `pnpm build` currently fails outside this plan's scope because `@ai-sdk/gateway` imports `lazyValidator` from `@ai-sdk/provider-utils`, but the installed package does not export that symbol. The failure occurs during Vite production bundling before this plan's changes are implicated.
