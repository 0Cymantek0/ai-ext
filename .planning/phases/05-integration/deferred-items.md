- `pnpm run lint` currently fails due pre-existing repo-wide issues outside plan 05-04 scope, including a parser config miss at `ai-extension/src/__tests__/contentCapture.capture.spec.ts` and widespread existing CRLF/prettier violations in files such as `ai-extension/src/background/abbreviation-storage.ts`.
- `pnpm build` currently fails in dependency code outside plan 05-04 scope: `@ai-sdk/gateway/dist/index.mjs` imports `lazyValidator` from `@ai-sdk/provider-utils`, but the installed `@ai-sdk/provider-utils` package does not export it.
