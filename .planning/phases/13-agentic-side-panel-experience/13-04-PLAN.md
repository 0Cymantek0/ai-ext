---
phase: 13-agentic-side-panel-experience
plan: 04
type: execute
wave: 3
depends_on: ["13-01", "13-02", "13-03"]
files_modified:
  - ai-extension/tests/sidepanel/use-agent-run-stream.test.tsx
  - ai-extension/tests/sidepanel/chat-app-agent-responsiveness.test.tsx
  - ai-extension/src/sidepanel/hooks/useAgentRunEvents.ts
  - ai-extension/src/sidepanel/ChatApp.tsx
autonomous: true
requirements: [UX-05]
must_haves:
  truths:
    - "Side panel remains responsive during long-running agent work"
    - "User can browse pockets and chat while active runs continue streaming updates"
    - "Timeline updates appear as events happen, without batching or delayed delivery"
    - "ChatApp no longer fan-outs full re-renders across unrelated sidepanel surfaces on every agent event"
  artifacts:
    - path: "ai-extension/src/sidepanel/hooks/useAgentRunEvents.ts"
      provides: "Bounded realtime run subscription hook with immediate per-event updates"
      exports: ["useAgentRunEvents"]
    - path: "ai-extension/src/sidepanel/ChatApp.tsx"
      provides: "Workflow-scoped run state and reduced render fan-out"
  key_links:
    - from: "ai-extension/src/sidepanel/hooks/useAgentRunEvents.ts"
      to: "ai-extension/src/shared/agent-runtime/selectors.ts"
      via: "selectAgentTimeline on bounded raw events"
      pattern: "selectAgentTimeline"
    - from: "ai-extension/src/sidepanel/ChatApp.tsx"
      to: "ai-extension/src/sidepanel/hooks/useAgentRunEvents.ts"
      via: "independent browser-action and deep-research subscriptions"
      pattern: "useAgentRunEvents"
---

<objective>
Fix the real UX-05 bottleneck in the side panel by reducing ChatApp render fan-out while preserving realtime timeline behavior. This plan must keep updates immediate per the locked live-timeline decision from `13-CONTEXT.md`; optimize rendering, not event delivery.

Purpose: Keep the side panel interactive during long runs without batching or delaying timeline updates.
Output: A bounded realtime hook plus ChatApp state isolation that prevents unrelated sidepanel surfaces from rerendering on every agent event.
</objective>

<execution_context>
@C:/Users/shuvagata/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/shuvagata/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-agentic-side-panel-experience/13-CONTEXT.md
@.planning/phases/13-agentic-side-panel-experience/13-02-PLAN.md
@ai-extension/src/sidepanel/ChatApp.tsx
@ai-extension/src/shared/agent-runtime/contracts.ts
@ai-extension/src/shared/agent-runtime/selectors.ts
@ai-extension/src/shared/types/index.d.ts
</context>

<interfaces>
From ai-extension/src/shared/agent-runtime/selectors.ts:
```typescript
export function selectAgentTimeline(events: AgentRunEvent[]): AgentTimelineEntry[];
export function selectAgentPanelState(
  run: AgentRun,
  events: AgentRunEvent[],
): AgentPanelState;
```

From ai-extension/src/shared/types/index.d.ts:
```typescript
export interface AgentRunStatusPayload {
  run: AgentRun;
  events?: AgentRunEvent[];
  checkpoints?: Array<
    Pick<AgentCheckpoint, "checkpointId" | "timestamp" | "trigger" | "boundary">
  >;
}

export interface AgentRunEventPayload {
  event: AgentRunEvent;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 0: Create Wave 0 tests for the streaming hook and ChatApp responsiveness</name>
  <files>
    - ai-extension/tests/sidepanel/use-agent-run-stream.test.tsx
    - ai-extension/tests/sidepanel/chat-app-agent-responsiveness.test.tsx
  </files>
  <read_first>
    - ai-extension/src/sidepanel/ChatApp.tsx
    - ai-extension/src/shared/types/index.d.ts
    - ai-extension/src/shared/agent-runtime/selectors.ts
  </read_first>
  <action>
    Create the missing Wave 0 tests required by `13-VALIDATION.md` before Plan 04 implementation verifies against them.

    Test requirements:
    - `use-agent-run-stream.test.tsx` must cover immediate hydration on run selection, per-event streaming updates, event deduplication, bounded retention, and selector-driven timeline projection for UX-05
    - `chat-app-agent-responsiveness.test.tsx` must cover workflow stream isolation plus continued chat/history/pocket interaction while agent events stream, matching the responsiveness goal without relying on fake timers as a substitute for real state isolation
    - Use the real `AGENT_RUN_STATUS` and `AGENT_RUN_EVENT` message shapes, shared runtime contracts, and current ChatApp composition boundaries rather than placeholder hook signatures
    - Keep the tests production-oriented so Tasks 1-2 can implement the hook and ChatApp refactor directly against them
  </action>
  <verify>
    <automated>cd ai-extension; (Test-Path tests/sidepanel/use-agent-run-stream.test.tsx) -and (Test-Path tests/sidepanel/chat-app-agent-responsiveness.test.tsx)</automated>
  </verify>
  <done>The two Wave 0 test files exist with production-quality UX-05 coverage for bounded streaming behavior and sidepanel responsiveness.</done>
</task>

<task type="auto">
  <name>Task 1: Create immediate, bounded per-run subscription hook</name>
  <files>ai-extension/src/sidepanel/hooks/useAgentRunEvents.ts</files>
  <read_first>
    - ai-extension/src/sidepanel/ChatApp.tsx
    - ai-extension/src/shared/types/index.d.ts
    - ai-extension/src/shared/agent-runtime/selectors.ts
    - ai-extension/src/shared/agent-runtime/contracts.ts
  </read_first>
  <acceptance_criteria>
    - `useAgentRunEvents(runId)` returns raw `events`, projected `timeline`, `run`, and `error`
    - Hook hydrates immediately on `runId` change by requesting the existing status/detail payload, then appends matching `AGENT_RUN_EVENT` messages one-by-one as they arrive
    - No timer-based batching, interval flushes, requestAnimationFrame coalescing, or delayed delivery logic is introduced
    - Event storage is bounded and deduplicated by `eventId`
    - `timeline` is derived with `selectAgentTimeline(events)` inside the hook so consumers do not repeat selector work
  </acceptance_criteria>
  <action>
    Create `useAgentRunEvents.ts` as the canonical subscription primitive for active runs.

    Implementation requirements:
    - Signature: `(runId: string | null) => { run: AgentRun | null; events: AgentRunEvent[]; timeline: AgentTimelineEntry[]; error: string | null }`
    - On `runId` change, request the existing status/detail payload for that run and seed `run` plus `events`
    - Subscribe to `chrome.runtime.onMessage`, accept only matching `AGENT_RUN_STATUS` and `AGENT_RUN_EVENT` payloads for the active `runId`, and update state immediately for each event
    - Keep a bounded buffer such as `MAX_EVENTS = 200`; discard oldest events after dedupe
    - Use selector-based memoization for `timeline` and any derived panel state, but do not batch message delivery
    - Clear all state cleanly when `runId` becomes `null`
  </action>
  <verify>
    <automated>cd ai-extension && pnpm exec vitest run tests/sidepanel/use-agent-run-stream.test.tsx && pnpm build</automated>
  </verify>
  <done>`useAgentRunEvents` hydrates and streams one run at a time with immediate updates, bounded memory, and selector-derived timeline output.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor ChatApp to isolate workflow streams and eliminate render fan-out</name>
  <files>ai-extension/src/sidepanel/ChatApp.tsx</files>
  <read_first>
    - ai-extension/src/sidepanel/ChatApp.tsx
    - ai-extension/src/sidepanel/hooks/useAgentRunEvents.ts
    - ai-extension/src/shared/agent-runtime/selectors.ts
    - ai-extension/src/sidepanel/components/WorkflowLauncher.tsx
    - ai-extension/src/sidepanel/components/RunReviewPanel.tsx
  </read_first>
  <acceptance_criteria>
    - `ChatApp.tsx` uses separate `useAgentRunEvents` instances for browser-action and deep-research instead of maintaining global event arrays plus `appendAgentEvent`
    - Workflow panels receive already-derived `timeline`, `run`, `panelState`, and `error` from their own stream, so updates for one workflow do not recompute the other workflow's derived state
    - Pocket browsing, chat history, and other non-agent sidepanel surfaces are not tied to the agent event listener path
    - Historical review state from Plan 02 remains isolated from live workflow state
    - The old render bottleneck surfaces (`browserActionTimeline`, `deepResearchTimeline`, shared event append fan-out) are removed or reduced to workflow-local derivation
  </acceptance_criteria>
  <action>
    Refactor `ChatApp.tsx` around workflow-scoped streaming state.

    Implementation requirements:
    - Replace the top-level raw event state and `appendAgentEvent` fan-out with `const browserActionStream = useAgentRunEvents(browserActionRunId)` and `const deepResearchStream = useAgentRunEvents(deepResearchRunId)`
    - Keep only lightweight launch/control state in ChatApp: active run IDs, current workflow choice, and historical review selection
    - Derive `panelState` with `selectAgentPanelState(browserActionStream.run, browserActionStream.events)` and the equivalent deep-research path near each workflow slice, not in a shared top-level event reducer
    - Pass each workflow panel only its own run/timeline/error props
    - Preserve all existing non-agent browsing/chat/history behavior while ensuring those surfaces do not subscribe to agent event updates directly
  </action>
  <verify>
    <automated>cd ai-extension && pnpm exec vitest run tests/sidepanel/use-agent-run-stream.test.tsx tests/sidepanel/chat-app-agent-responsiveness.test.tsx && pnpm build</automated>
  </verify>
  <done>`ChatApp.tsx` no longer fan-outs agent event updates across unrelated UI, and the side panel stays interactive while runs continue to stream.</done>
</task>

</tasks>

<verification>
- Timeline updates arrive as events happen; no batching or delayed delivery path exists
- Browser-action and deep-research streams are isolated from each other
- Pocket/chat/history interactions remain usable during a long active run
- `cd ai-extension && pnpm build` completes without errors
</verification>

<success_criteria>
- UX-05: side panel remains responsive during long-running sessions
- Live updates stay realtime per the locked timeline decision in `13-CONTEXT.md`
- ChatApp render fan-out is reduced enough that unrelated browsing/chat surfaces stay interactive
- Build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/13-agentic-side-panel-experience/13-04-SUMMARY.md`
</output>
