---
phase: 13-agentic-side-panel-experience
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - ai-extension/tests/sidepanel/agent-timeline.test.tsx
  - ai-extension/tests/sidepanel/run-history-panel.test.tsx
  - ai-extension/tests/sidepanel/run-review-panel.test.tsx
  - ai-extension/src/sidepanel/components/AgentTimeline.tsx
  - ai-extension/src/sidepanel/components/AgentTimelineEntry.tsx
  - ai-extension/src/sidepanel/components/RunHistoryPanel.tsx
  - ai-extension/src/sidepanel/components/RunHistoryItem.tsx
  - ai-extension/src/sidepanel/components/RunReviewPanel.tsx
  - ai-extension/src/sidepanel/components/BrowserActionPanel.tsx
  - ai-extension/src/sidepanel/components/DeepResearchPanel.tsx
  - ai-extension/src/sidepanel/ChatApp.tsx
  - ai-extension/src/shared/types/index.d.ts
  - ai-extension/src/background/service-worker.ts
autonomous: true
requirements: [UX-02, UX-04]
must_haves:
  truths:
    - "Active browser-action and deep-research runs show the same readable activity timeline pattern"
    - "Users can browse completed runs from the sidepanel without leaving their current workspace context"
    - "Selecting a completed run opens a review surface that explains what happened, what was collected, and how the run ended"
    - "Historical review language stays consistent with the live timeline so users do not see conflicting event descriptions"
  artifacts:
    - path: "ai-extension/src/sidepanel/components/AgentTimeline.tsx"
      provides: "Shared timeline container for live and historical run review"
      exports: ["AgentTimeline"]
    - path: "ai-extension/src/sidepanel/components/AgentTimelineEntry.tsx"
      provides: "Timeline row renderer driven by projected AgentTimelineEntry data"
      exports: ["AgentTimelineEntry"]
    - path: "ai-extension/src/sidepanel/components/RunHistoryPanel.tsx"
      provides: "Typed historical run browser with filtering and selection"
      exports: ["RunHistoryPanel"]
    - path: "ai-extension/src/sidepanel/components/RunReviewPanel.tsx"
      provides: "Hydrated review surface for completed runs"
      exports: ["RunReviewPanel"]
    - path: "ai-extension/src/background/service-worker.ts"
      provides: "Typed message handlers for history list and run detail hydration"
  key_links:
    - from: "ai-extension/src/sidepanel/ChatApp.tsx"
      to: "ai-extension/src/background/service-worker.ts"
      via: "typed history/review requests"
      pattern: "AGENT_RUN_HISTORY_(LIST|DETAIL)"
    - from: "ai-extension/src/background/service-worker.ts"
      to: "ai-extension/src/background/agent-runtime/agent-runtime-service.ts"
      via: "agentRuntimeService.listRuns and agentRuntimeService.getTimeline"
      pattern: "listRuns|getTimeline"
    - from: "ai-extension/src/sidepanel/ChatApp.tsx"
      to: "ai-extension/src/shared/agent-runtime/selectors.ts"
      via: "selectAgentTimeline on hydrated raw events"
      pattern: "selectAgentTimeline"
---

<objective>
Build the shared timeline and historical review flow for the side panel without inventing new storage access patterns. This plan covers UX-02 and UX-04 by using typed background handlers plus canonical selectors for both live and historical event rendering.

Purpose: Replace inline timeline previews, add real run-history retrieval through the background runtime, and make completed runs inspectable in a fully hydrated review surface.
Output: Shared timeline components, typed history/detail message handlers, run history browsing UI, and ChatApp wiring for historical review.
</objective>

<execution_context>
@C:/Users/shuvagata/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/shuvagata/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-agentic-side-panel-experience/13-CONTEXT.md
@.planning/phases/13-agentic-side-panel-experience/13-01-PLAN.md
@ai-extension/src/shared/agent-runtime/contracts.ts
@ai-extension/src/shared/agent-runtime/selectors.ts
@ai-extension/src/shared/types/index.d.ts
@ai-extension/src/background/agent-runtime/agent-runtime-service.ts
@ai-extension/src/background/service-worker.ts
</context>

<interfaces>
<!-- Use existing runtime and selector contracts; do not bypass them with direct sidepanel store access. -->

From ai-extension/src/background/agent-runtime/agent-runtime-service.ts:
```typescript
export class AgentRuntimeService {
  async getTimeline(runId: string): Promise<AgentRunTimeline | undefined>;
  async listRuns(status?: AgentRunStatus): Promise<AgentRun[]>;
  async listRunsByMode(mode: AgentRunMode): Promise<AgentRun[]>;
}
```

From ai-extension/src/shared/types/index.d.ts:
```typescript
export interface AgentRunStatusPayload {
  run: AgentRun;
  events?: AgentRunEvent[];
  checkpoints?: Array<
    Pick<AgentCheckpoint, "checkpointId" | "timestamp" | "trigger" | "boundary">
  >;
}
```

From ai-extension/src/shared/agent-runtime/selectors.ts:
```typescript
export function selectAgentTimeline(events: AgentRunEvent[]): AgentTimelineEntry[];

interface AgentTimelineEntry {
  eventId: string;
  runId: string;
  type: string;
  timestamp: number;
  label: string;
  detail?: string;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 0: Create Wave 0 tests for shared timeline and historical review flows</name>
  <files>
    - ai-extension/tests/sidepanel/agent-timeline.test.tsx
    - ai-extension/tests/sidepanel/run-history-panel.test.tsx
    - ai-extension/tests/sidepanel/run-review-panel.test.tsx
  </files>
  <read_first>
    - ai-extension/src/shared/agent-runtime/selectors.ts
    - ai-extension/src/background/service-worker.ts
    - ai-extension/src/sidepanel/ChatApp.tsx
  </read_first>
  <action>
    Create the missing Wave 0 production-quality tests required by `13-VALIDATION.md` before Plan 02 implementation tasks verify against them.

    Test requirements:
    - `agent-timeline.test.tsx` must cover compact and full timeline rendering, newest-first ordering, empty state behavior, and the contract that display text comes from projected `label/detail` fields rather than raw event parsing
    - `run-history-panel.test.tsx` must cover loading/filtering completed runs, mode-aware browsing, and selection callbacks using typed history payloads
    - `run-review-panel.test.tsx` must cover hydrated review rendering for run metadata, projected timeline, artifacts/evidence context, terminal outcome, and missing-run empty/error handling
    - Use real `AgentRun`, `AgentRunEvent`, and typed message contract shapes from the shared runtime; do not use fake placeholder assertions or ad hoc event schemas
    - Write the tests as executable specs for UX-02 and UX-04 so the downstream component tasks can satisfy them directly
  </action>
  <verify>
    <automated>cd ai-extension; (Test-Path tests/sidepanel/agent-timeline.test.tsx) -and (Test-Path tests/sidepanel/run-history-panel.test.tsx) -and (Test-Path tests/sidepanel/run-review-panel.test.tsx)</automated>
  </verify>
  <done>The three Wave 0 test files exist with production-ready assertions that define the shared timeline and historical review behavior for UX-02 and UX-04.</done>
</task>

<task type="auto">
  <name>Task 1: Create shared timeline primitives driven by projected selector output</name>
  <files>
    - ai-extension/src/sidepanel/components/AgentTimelineEntry.tsx
    - ai-extension/src/sidepanel/components/AgentTimeline.tsx
  </files>
  <read_first>
    - ai-extension/src/shared/agent-runtime/selectors.ts
    - ai-extension/src/sidepanel/ChatApp.tsx
    - ai-extension/src/lib/utils.ts
  </read_first>
  <acceptance_criteria>
    - `AgentTimelineEntry.tsx` accepts `AgentTimelineEntry` data only and does not inspect raw `AgentRunEvent` payloads
    - `AgentTimelineEntry.tsx` maps `entry.type` to icon/tone but uses `entry.label` and `entry.detail` for display text instead of rebuilding event semantics
    - `AgentTimeline.tsx` accepts `AgentTimelineEntry[]`, supports `maxItems`, `title`, `collapsible`, and an empty state
    - Timeline rendering is reusable for both live panels and historical review surfaces
    - Both components follow existing sidepanel Tailwind/lucide patterns
  </acceptance_criteria>
  <action>
    Create `AgentTimelineEntry.tsx` and `AgentTimeline.tsx` as the single timeline rendering path for UX-02 and UX-04.

    Requirements:
    - `AgentTimelineEntry` props: `entry: AgentTimelineEntry`, optional `className`
    - Use `entry.type` only for icon/color selection; render human-readable text from `entry.label` and `entry.detail`
    - Do not parse raw event payload fields such as tool names, approval reasons, or evidence metadata inside the component; that projection already belongs to `selectAgentTimeline()`
    - `AgentTimeline` must sort newest-first for display, cap items when `maxItems` is provided, and support a compact preview mode for live panels plus full mode for run review
    - Keep the container visually consistent with the existing agent panels from Plan 01
  </action>
  <verify>
    <automated>cd ai-extension && pnpm exec vitest run tests/sidepanel/agent-timeline.test.tsx tests/sidepanel/browser-action-timeline.test.tsx tests/sidepanel/deep-research-timeline.test.tsx && pnpm build</automated>
  </verify>
  <done>Shared timeline components exist, consume projected timeline entries only, and can render both compact live previews and full historical timelines.</done>
</task>

<task type="auto">
  <name>Task 2: Add typed run-history transport and hydrated review components</name>
  <files>
    - ai-extension/src/shared/types/index.d.ts
    - ai-extension/src/background/service-worker.ts
    - ai-extension/src/sidepanel/components/RunHistoryItem.tsx
    - ai-extension/src/sidepanel/components/RunHistoryPanel.tsx
    - ai-extension/src/sidepanel/components/RunReviewPanel.tsx
  </files>
  <read_first>
    - ai-extension/src/shared/types/index.d.ts
    - ai-extension/src/background/service-worker.ts
    - ai-extension/src/background/agent-runtime/agent-runtime-service.ts
    - ai-extension/src/sidepanel/components/AgentRunStatusBadge.tsx
    - ai-extension/src/sidepanel/components/AgentTimeline.tsx
  </read_first>
  <acceptance_criteria>
    - Shared message contracts define typed request/response payloads for listing historical runs and fetching one run's hydrated detail
    - Service worker handlers use `agentRuntimeService.listRuns()` and `agentRuntimeService.getTimeline()`; no plan step references direct sidepanel IndexedDB access
    - `RunHistoryPanel` loads runs through the new typed message handler, supports mode filtering, and renders `RunHistoryItem`
    - `RunReviewPanel` renders run metadata, terminal outcome, projected timeline, artifact refs, and an evidence section derived from hydrated run detail
    - Historical deep-research runs show evidence/artifact context, and browser-action runs show terminal outcome and saved artifacts without placeholder data
  </acceptance_criteria>
  <action>
    Add typed message handlers for historical run review, then build the components that consume them.

    Transport requirements:
    - Extend `MessageKind` and payload types in `src/shared/types/index.d.ts` with explicit history list/detail messages, for example `AGENT_RUN_HISTORY_LIST` and `AGENT_RUN_HISTORY_DETAIL`
    - The list response must return `AgentRun[]`
    - The detail response must return `run`, raw `events`, and `checkpoints` using the same shape as `AgentRunStatusPayload`
    - In `service-worker.ts`, implement handlers that delegate to `agentRuntimeService.listRuns()` and `agentRuntimeService.getTimeline()`
    - Do not introduce `AGENT_RUN_LIST`, do not query IndexedDB from the side panel, and do not invent alternate storage contracts

    UI requirements:
    - `RunHistoryItem.tsx` shows mode, status, summary text, duration, and timestamp
    - `RunHistoryPanel.tsx` is the browse/filter surface only
    - `RunReviewPanel.tsx` is the selected-run review surface with sections for: summary/status, timeline, artifact refs, evidence-related activity, and terminal outcome
    - `RunReviewPanel` should accept hydrated data from ChatApp rather than fetching on its own
  </action>
  <verify>
    <automated>cd ai-extension && pnpm exec vitest run tests/sidepanel/run-history-panel.test.tsx tests/sidepanel/run-review-panel.test.tsx && pnpm build</automated>
  </verify>
  <done>Typed history transport exists, the service worker delegates to `AgentRuntimeService`, and dedicated history/review components can present completed runs with artifacts, evidence, and terminal outcome.</done>
</task>

<task type="auto">
  <name>Task 3: Wire live panels and historical selection through canonical selectors</name>
  <files>
    - ai-extension/src/sidepanel/components/BrowserActionPanel.tsx
    - ai-extension/src/sidepanel/components/DeepResearchPanel.tsx
    - ai-extension/src/sidepanel/ChatApp.tsx
  </files>
  <read_first>
    - ai-extension/src/sidepanel/components/BrowserActionPanel.tsx
    - ai-extension/src/sidepanel/components/DeepResearchPanel.tsx
    - ai-extension/src/sidepanel/components/AgentTimeline.tsx
    - ai-extension/src/sidepanel/components/RunHistoryPanel.tsx
    - ai-extension/src/sidepanel/components/RunReviewPanel.tsx
    - ai-extension/src/shared/agent-runtime/selectors.ts
  </read_first>
  <acceptance_criteria>
    - `BrowserActionPanel` and `DeepResearchPanel` render `AgentTimeline` instead of inline timeline previews
    - `ChatApp.tsx` exposes a run-history affordance, opens `RunHistoryPanel`, and loads the selected run detail through the typed background handler
    - On historical selection, `ChatApp.tsx` calls `selectAgentTimeline(hydrated.events)` and passes the projected timeline into `RunReviewPanel`
    - `ChatApp.tsx` does not rebuild event label/detail logic in UI code
    - The selected historical run remains inspectable even after the panel is reopened, without overwriting the active live workflow state
  </acceptance_criteria>
  <action>
    Replace the remaining inline timeline preview usage and wire historical inspection into `ChatApp.tsx`.

    Implementation requirements:
    - In both workflow panels, replace the inline last-5-event JSX with `<AgentTimeline events={events} ... />`
    - In `ChatApp.tsx`, add state for `isRunHistoryOpen`, `selectedHistoricalRun`, `selectedHistoricalEvents`, and `selectedHistoricalCheckpoints`
    - Add a `handleSelectHistoricalRun(runId)` callback that sends the typed detail message, stores the hydrated response, derives `selectedHistoricalTimeline` with `selectAgentTimeline(hydrated.events)`, and opens `RunReviewPanel`
    - Keep live browser-action and deep-research state independent from historical review state; selecting a completed run should not clobber the currently active live run
    - If the selected run is missing, show a clear inline empty/error state in the review surface instead of silently closing
  </action>
  <verify>
    <automated>cd ai-extension && pnpm exec vitest run tests/sidepanel/agent-timeline.test.tsx tests/sidepanel/run-history-panel.test.tsx tests/sidepanel/run-review-panel.test.tsx && pnpm build</automated>
  </verify>
  <done>Live panels use the shared timeline, ChatApp loads historical runs through typed handlers, and selecting a completed run opens a hydrated review surface driven by `selectAgentTimeline()` plus the run's artifacts and terminal outcome.</done>
</task>

</tasks>

<verification>
- Live browser-action and deep-research panels render the shared `AgentTimeline`
- Historical run list is loaded via typed background messages backed by `AgentRuntimeService`
- Selecting a run opens a review surface with timeline, artifacts/evidence, and terminal outcome
- Historical review uses `selectAgentTimeline()` instead of ad hoc UI event parsing
- `cd ai-extension && pnpm build` completes without errors
</verification>

<success_criteria>
- UX-02: human-readable live timeline is shared across both workflows
- UX-04: completed runs are browsable and reopen into a fully hydrated review surface
- No plan step relies on nonexistent `AGENT_RUN_LIST` or direct sidepanel IndexedDB reads
- Build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/13-agentic-side-panel-experience/13-02-SUMMARY.md`
</output>
