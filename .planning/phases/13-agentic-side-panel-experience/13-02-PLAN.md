---
phase: 13-agentic-side-panel-experience
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - ai-extension/src/sidepanel/components/AgentTimeline.tsx
  - ai-extension/src/sidepanel/components/AgentTimelineEntry.tsx
  - ai-extension/src/sidepanel/components/RunHistoryPanel.tsx
  - ai-extension/src/sidepanel/components/RunHistoryItem.tsx
  - ai-extension/src/sidepanel/ChatApp.tsx
autonomous: true
requirements: [UX-02, UX-04]
must_haves:
  truths:
    - "Timeline renders as a dedicated component, not inline JSX"
    - "Timeline entries are human-readable with event-specific formatting"
    - "Historical runs are accessible in a dedicated panel"
    - "User can open a historical run and see what happened"
  artifacts:
    - path: "ai-extension/src/sidepanel/components/AgentTimeline.tsx"
      provides: "Scrollable timeline container with event list rendering"
      exports: ["AgentTimeline"]
    - path: "ai-extension/src/sidepanel/components/AgentTimelineEntry.tsx"
      provides: "Individual timeline entry with event-type-specific rendering"
      exports: ["AgentTimelineEntry"]
    - path: "ai-extension/src/sidepanel/components/RunHistoryPanel.tsx"
      provides: "Panel listing historical agent runs with filtering"
      exports: ["RunHistoryPanel"]
    - path: "ai-extension/src/sidepanel/components/RunHistoryItem.tsx"
      provides: "Individual historical run item with summary info"
      exports: ["RunHistoryItem"]
  key_links:
    - from: "ai-extension/src/sidepanel/components/BrowserActionPanel.tsx"
      to: "ai-extension/src/sidepanel/components/AgentTimeline.tsx"
      via: "import and render"
      pattern: "import.*AgentTimeline"
    - from: "ai-extension/src/sidepanel/components/DeepResearchPanel.tsx"
      to: "ai-extension/src/sidepanel/components/AgentTimeline.tsx"
      via: "import and render"
      pattern: "import.*AgentTimeline"
    - from: "ai-extension/src/sidepanel/ChatApp.tsx"
      to: "ai-extension/src/sidepanel/components/RunHistoryPanel.tsx"
      via: "import and render"
      pattern: "import.*RunHistoryPanel"
---

<objective>
Build a reusable AgentTimeline component for live event display and a RunHistoryPanel for inspecting completed runs. This addresses UX-02 (live timeline) and UX-04 (historical run inspection).

Purpose: Replace inline timeline previews in panel components with a proper timeline component that handles all event types. Add run history for post-completion review.
Output: Four new component files + ChatApp.tsx integration for run history access.
</objective>

<execution_context>
@C:/Users/shuvagata/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/shuvagata/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-agentic-side-panel-experience/13-CONTEXT.md
@.planning/phases/13-agentic-side-panel-experience/13-01-PLAN.md
@ai-extension/src/shared/agent-runtime/contracts.ts
@ai-extension/src/shared/agent-runtime/selectors.ts
@ai-extension/src/background/agent-runtime/store.ts
</context>

<interfaces>
<!-- Key types the executor needs. Extracted from codebase. -->

From ai-extension/src/shared/agent-runtime/contracts.ts:
```typescript
type AgentRunEvent =
  | RunStartedEvent | RunImportedEvent | RunPhaseChangedEvent
  | TodoReplacedEvent | TodoItemUpsertedEvent
  | ToolCalledEvent | ToolCompletedEvent | ToolFailedEvent
  | ApprovalRequestedEvent | ApprovalResolvedEvent
  | ArtifactProjectedEvent | EvidenceRecordedEvent
  | CheckpointCreatedEvent
  | RunCompletedEvent | RunFailedEvent | RunCancelledEvent;

interface AgentRun {
  runId: string;
  mode: AgentRunMode;
  status: AgentRunStatus;
  phase: AgentRunPhase;
  createdAt: number;
  updatedAt: number;
  terminalOutcome: AgentTerminalOutcome | null;
  metadata: Record<string, unknown>;
}
```

From ai-extension/src/shared/agent-runtime/selectors.ts:
```typescript
interface AgentTimelineEntry {
  eventId: string;
  runId: string;
  type: string;
  timestamp: number;
  label: string;
  detail?: string;
}
```

From ai-extension/src/background/agent-runtime/store.ts:
```typescript
// IndexedDB stores used:
// AGENT_RUNS — full run objects
// AGENT_RUN_EVENTS — all events for all runs
// Query pattern: get all events for a runId, sorted by timestamp
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create AgentTimelineEntry component for individual event rendering</name>
  <files>ai-extension/src/sidepanel/components/AgentTimelineEntry.tsx</files>
  <read_first>
    - ai-extension/src/shared/agent-runtime/contracts.ts (event type definitions)
    - ai-extension/src/sidepanel/ChatApp.tsx (see current inline timeline rendering patterns)
    - ai-extension/src/lib/utils.ts (cn utility)
  </read_first>
  <acceptance_criteria>
    - File ai-extension/src/sidepanel/components/AgentTimelineEntry.tsx exists
    - Exports AgentTimelineEntryProps interface with entry: AgentTimelineEntry
    - Exports AgentTimelineEntry as React.FC<AgentTimelineEntryProps>
    - Renders different UI based on entry.type:
      - "run.started": Shows "Run started" with timestamp
      - "run.phase_changed": Shows "Phase: {phase}" with phase name
      - "tool.called": Shows "Calling {toolName}" with lucide Wrench icon
      - "tool.completed": Shows "{toolName} completed" with lucide Check icon
      - "tool.failed": Shows "{toolName} failed" with lucide X icon and red styling
      - "approval.requested": Shows "Approval requested: {reason}" with lucide AlertTriangle icon
      - "approval.resolved": Shows "Approval {resolution}" with lucide CheckCircle or XCircle icon
      - "artifact.projected": Shows "Artifact saved: {name}" with lucide File icon
      - "evidence.recorded": Shows "Evidence recorded" with lucide BookOpen icon
      - "run.completed": Shows "Run completed" with lucide CheckCircle icon and green styling
      - "run.failed": Shows "Run failed: {error}" with lucide AlertCircle icon and red styling
      - "run.cancelled": Shows "Run cancelled" with lucide XCircle icon
    - Each entry shows timestamp formatted as relative time (e.g., "2m ago") or absolute time
    - Uses Tailwind classes matching existing ChatApp patterns
    - TypeScript compilation passes: pnpm --filter ai-extension exec tsc --noEmit
  </acceptance_criteria>
  <action>
    Create a component that renders a single timeline entry with event-type-specific styling and content.

    File: ai-extension/src/sidepanel/components/AgentTimelineEntry.tsx

    Props interface:
    ```typescript
    interface AgentTimelineEntryProps {
      entry: AgentTimelineEntry;
      className?: string;
    }
    ```

    Implementation:
    - Use a switch statement or record mapping on entry.type to determine icon, label, and styling
    - Import icons from lucide-react: Wrench, Check, X, AlertTriangle, CheckCircle, XCircle, File, BookOpen, AlertCircle, Play, Pause, RotateCcw
    - For each event type, render:
      - Left side: icon with color coding (green for success, red for failure, orange for warnings/approvals, blue for info)
      - Middle: human-readable label from entry.label (or derived from entry.type + entry.detail)
      - Right side: timestamp formatted as relative time (use a simple helper: if < 1 hour show "Xm ago", if < 24 hours show "Xh ago", else show date)
    - Use Tailwind classes for layout: "flex items-center gap-2 py-1.5 text-sm"
    - Color coding via text-* classes: text-emerald-500 (success), text-red-500 (failure), text-amber-500 (warning/approval), text-blue-500 (info)
    - Use cn() for className merging

    Event type to icon/color mapping (use these exact values):
    - run.started: Play, text-blue-500
    - run.phase_changed: RotateCcw, text-blue-500
    - tool.called: Wrench, text-blue-500
    - tool.completed: Check, text-emerald-500
    - tool.failed: X, text-red-500
    - approval.requested: AlertTriangle, text-amber-500
    - approval.resolved (approved): CheckCircle, text-emerald-500
    - approval.resolved (rejected): XCircle, text-red-500
    - artifact.projected: File, text-blue-500
    - evidence.recorded: BookOpen, text-emerald-500
    - run.completed: CheckCircle, text-emerald-500
    - run.failed: AlertCircle, text-red-500
    - run.cancelled: XCircle, text-gray-500
  </action>
  <verify>
    <automated>pnpm --filter ai-extension exec tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>AgentTimelineEntry.tsx exists with event-type-specific rendering for all 13 event types, proper icon/color mapping, timestamp formatting, and Tailwind styling. Component compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Create AgentTimeline container component</name>
  <files>ai-extension/src/sidepanel/components/AgentTimeline.tsx</files>
  <read_first>
    - ai-extension/src/sidepanel/components/AgentTimelineEntry.tsx (from Task 1)
    - ai-extension/src/sidepanel/ChatApp.tsx (see current inline timeline rendering)
    - ai-extension/src/shared/agent-runtime/selectors.ts (AgentTimelineEntry type)
  </read_first>
  <acceptance_criteria>
    - File ai-extension/src/sidepanel/components/AgentTimeline.tsx exists
    - Exports AgentTimelineProps interface with events: AgentTimelineEntry[], maxItems?: number, title?: string, collapsible?: boolean
    - Exports AgentTimeline as React.FC<AgentTimelineProps>
    - Renders a scrollable container with max-height (max-h-48 for preview, configurable)
    - Renders AgentTimelineEntry for each event in reverse chronological order (newest first)
    - Supports maxItems prop to limit displayed entries (default: show all)
    - Optional title header when title prop provided
    - Optional collapsible behavior when collapsible=true (collapsed by default, expand on click)
    - Shows "No events yet" empty state when events array is empty
    - TypeScript compilation passes: pnpm --filter ai-extension exec tsc --noEmit
  </acceptance_criteria>
  <action>
    Create a scrollable timeline container that renders a list of AgentTimelineEntry components.

    File: ai-extension/src/sidepanel/components/AgentTimeline.tsx

    Props interface:
    ```typescript
    interface AgentTimelineProps {
      events: AgentTimelineEntry[];
      maxItems?: number;
      title?: string;
      collapsible?: boolean;
      className?: string;
    }
    ```

    Implementation:
    - Sort events by timestamp descending (newest first): [...events].sort((a, b) => b.timestamp - a.timestamp)
    - If maxItems provided, slice to first N entries
    - If collapsible=true, use useState for isExpanded (default false), show expand/collapse toggle
    - Container: "flex flex-col gap-1 overflow-y-auto" with max-h-48 for scroll
    - If events empty: show "No events yet" in muted text, centered
    - If title provided: render title as "text-xs font-medium text-muted-foreground mb-1"
    - Each entry: render <AgentTimelineEntry entry={e} key={e.eventId} />
    - Use Tailwind classes matching existing ChatApp patterns

    This component replaces the inline timeline preview code currently in ChatApp.tsx.
  </action>
  <verify>
    <automated>pnpm --filter ai-extension exec tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>AgentTimeline.tsx exists with scrollable container, reverse chronological sorting, maxItems support, collapsible option, empty state, and AgentTimelineEntry rendering. Component compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Create RunHistoryItem component</name>
  <files>ai-extension/src/sidepanel/components/RunHistoryItem.tsx</files>
  <read_first>
    - ai-extension/src/shared/agent-runtime/contracts.ts (AgentRun type, terminalOutcome)
    - ai-extension/src/sidepanel/components/AgentRunStatusBadge.tsx (existing badge patterns)
    - ai-extension/src/lib/utils.ts (cn utility)
  </read_first>
  <acceptance_criteria>
    - File ai-extension/src/sidepanel/components/RunHistoryItem.tsx exists
    - Exports RunHistoryItemProps interface with run: AgentRun, onSelect: (runId: string) => void
    - Exports RunHistoryItem as React.FC<RunHistoryItemProps>
    - Displays: run mode icon (browser-action or deep-research), status badge, brief description, duration, timestamp
    - Browser action description: derived from metadata.task (truncated to 60 chars)
    - Deep research description: derived from metadata.topic (truncated to 60 chars)
    - Duration: calculated from updatedAt - createdAt, formatted as "Xm" or "Xh Ym"
    - Clickable card with hover state
    - Uses AgentRunStatusBadge for status display
    - TypeScript compilation passes: pnpm --filter ai-extension exec tsc --noEmit
  </acceptance_criteria>
  <action>
    Create a component that displays a single historical run as a clickable card.

    File: ai-extension/src/sidepanel/components/RunHistoryItem.tsx

    Props interface:
    ```typescript
    interface RunHistoryItemProps {
      run: AgentRun;
      onSelect: (runId: string) => void;
    }
    ```

    Implementation:
    - Container: clickable div with "flex items-start gap-3 p-3 rounded-lg border border-border/50 hover:bg-accent/50 cursor-pointer transition-colors"
    - Left: mode icon — Globe for browser-action, Search for deep-research (from lucide-react), sized w-5 h-5, text-muted-foreground
    - Middle: 
      - Top line: brief description (task or topic from metadata, truncated to 60 chars with "..." suffix)
      - Second line: duration (formatDuration helper: if < 60s show "Xs", if < 60m show "Xm", else "Xh Ym")
      - Third line: timestamp formatted as relative time or date
    - Right: AgentRunStatusBadge with the run's status
    - onClick: calls props.onSelect(run.runId)
    - Use metadata from run: for browser-action use (metadata as any).task, for deep-research use (metadata as any).topic
  </action>
  <verify>
    <automated>pnpm --filter ai-extension exec tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>RunHistoryItem.tsx exists with clickable card showing mode icon, description, duration, timestamp, and status badge. Component compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 4: Create RunHistoryPanel for browsing completed runs</name>
  <files>ai-extension/src/sidepanel/components/RunHistoryPanel.tsx</files>
  <read_first>
    - ai-extension/src/sidepanel/components/RunHistoryItem.tsx (from Task 3)
    - ai-extension/src/shared/agent-runtime/contracts.ts (AgentRun type)
    - ai-extension/src/background/agent-runtime/store.ts (how to query runs from IndexedDB)
    - ai-extension/src/shared/types/index.d.ts (MessageKind for loading runs)
  </read_first>
  <acceptance_criteria>
    - File ai-extension/src/sidepanel/components/RunHistoryPanel.tsx exists
    - Exports RunHistoryPanelProps interface with isOpen: boolean, onClose: () => void, onSelectRun: (runId: string) => void
    - Exports RunHistoryPanel as React.FC<RunHistoryPanelProps>
    - Slide-over panel (w-80, fixed right side, similar to HistoryPanel pattern)
    - Loads historical runs via chrome.runtime.sendMessage with AGENT_RUN_LIST message kind (or queries AgentRuntimeStore directly)
    - Renders list of RunHistoryItem components sorted by createdAt descending
    - Filter by mode (All, Browser Action, Deep Research) via tabs
    - Shows loading state while fetching
    - Shows empty state "No runs yet" when no runs exist
    - Close button in header
    - Uses framer-motion for slide animation (matching HistoryPanel patterns)
    - TypeScript compilation passes: pnpm --filter ai-extension exec tsc --noEmit
  </acceptance_criteria>
  <action>
    Create a slide-over panel that lists all historical agent runs, filterable by mode.

    File: ai-extension/src/sidepanel/components/RunHistoryPanel.tsx

    Props interface:
    ```typescript
    interface RunHistoryPanelProps {
      isOpen: boolean;
      onClose: () => void;
      onSelectRun: (runId: string) => void;
    }
    ```

    Implementation:
    - Panel container: fixed position, w-80, right-aligned, full height, z-50, matching HistoryPanel styling
    - Header: "Run History" title with close button (X icon from lucide-react)
    - Filter tabs: "All" | "Browser Action" | "Deep Research" — use useState for activeFilter
    - Load runs: useEffect on isOpen, send chrome.runtime.sendMessage({ kind: "AGENT_RUN_LIST" }) or query IndexedDB directly via AgentRuntimeStore
    - State: runs (AgentRun[]), isLoading (boolean)
    - Filter runs by mode based on activeFilter
    - Sort by createdAt descending
    - Render RunHistoryItem for each run
    - Loading state: "Loading runs..." with a simple spinner or pulsing dots
    - Empty state: "No runs yet. Start a browser action or deep research to see it here."
    - Use framer-motion AnimatePresence for slide-in/out animation (matching HistoryPanel)
    - Backdrop: semi-transparent overlay, click to close

    Message kind note: If "AGENT_RUN_LIST" doesn't exist in MessageKind, the executor should add it to the shared types or use an alternative approach (query IndexedDB directly from the side panel using the same stores as the service worker).
  </action>
  <verify>
    <automated>pnpm --filter ai-extension exec tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>RunHistoryPanel.tsx exists with slide-over panel, run loading, mode filtering, RunHistoryItem list, loading/empty states, and framer-motion animation. Component compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 5: Wire timeline and history into ChatApp and panel components</name>
  <files>
    - ai-extension/src/sidepanel/components/BrowserActionPanel.tsx
    - ai-extension/src/sidepanel/components/DeepResearchPanel.tsx
    - ai-extension/src/sidepanel/ChatApp.tsx
  </files>
  <read_first>
    - ai-extension/src/sidepanel/components/BrowserActionPanel.tsx (from plan 01)
    - ai-extension/src/sidepanel/components/DeepResearchPanel.tsx (from plan 01)
    - ai-extension/src/sidepanel/ChatApp.tsx
    - ai-extension/src/sidepanel/components/AgentTimeline.tsx (from Task 2)
    - ai-extension/src/sidepanel/components/RunHistoryPanel.tsx (from Task 4)
  </read_first>
  <acceptance_criteria>
    - BrowserActionPanel.tsx imports and renders AgentTimeline instead of inline timeline preview
    - DeepResearchPanel.tsx imports and renders AgentTimeline instead of inline timeline preview
    - ChatApp.tsx imports RunHistoryPanel
    - ChatApp.tsx has isRunHistoryOpen state and toggle handler
    - ChatApp.tsx renders RunHistoryPanel when isRunHistoryOpen is true
    - TopBar or appropriate location has a "Run History" button that opens the panel
    - onSelectRun callback navigates to ai-pocket mode and shows the selected run details
    - TypeScript compilation passes: pnpm --filter ai-extension exec tsc --noEmit
  </acceptance_criteria>
  <action>
    Integrate the new timeline and run history components into the existing UI.

    Step 1 — Update BrowserActionPanel.tsx:
    - Import AgentTimeline from "./AgentTimeline"
    - Replace the inline timeline preview (last 5 events) with:
      ```tsx
      <AgentTimeline events={events} maxItems={5} title="Recent Activity" collapsible />
      ```

    Step 2 — Update DeepResearchPanel.tsx:
    - Import AgentTimeline from "./AgentTimeline"
    - Replace the inline timeline preview (last 5 events) with:
      ```tsx
      <AgentTimeline events={events} maxItems={5} title="Recent Activity" collapsible />
      ```

    Step 3 — Update ChatApp.tsx:
    - Import RunHistoryPanel from "./components/RunHistoryPanel"
    - Add state: const [isRunHistoryOpen, setIsRunHistoryOpen] = useState(false)
    - Add to TopBar: a "Run History" button (clock icon from lucide-react) that calls setIsRunHistoryOpen(true)
    - Render RunHistoryPanel near the HistoryPanel:
      ```tsx
      <RunHistoryPanel
        isOpen={isRunHistoryOpen}
        onClose={() => setIsRunHistoryOpen(false)}
        onSelectRun={handleSelectRun}
      />
      ```
    - Add handleSelectRun callback:
      ```typescript
      const handleSelectRun = useCallback((runId: string) => {
        // Load the run details and show in the appropriate panel
        // For now: close history, switch to ask mode, the run state will be loaded
        setIsRunHistoryOpen(false);
        // The run details will be shown via existing browserActionRun/deepResearchRun state
        // Full implementation: fetch run from store and set appropriate state
      }, []);
      ```

    Step 4 — Verify all imports resolve and TypeScript compiles.
  </action>
  <verify>
    <automated>pnpm --filter ai-extension exec tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>AgentTimeline integrated into both panel components. RunHistoryPanel integrated into ChatApp with toggle button and selection handler. All components compile cleanly.</done>
</task>

</tasks>

<verification>
- AgentTimelineEntry renders all 13 event types with correct icons and colors
- AgentTimeline displays events in reverse chronological order with scroll, maxItems, collapsible
- RunHistoryItem shows run summary with mode, description, duration, status
- RunHistoryPanel loads and filters historical runs with slide-over animation
- Timeline components wired into BrowserActionPanel and DeepResearchPanel
- RunHistoryPanel wired into ChatApp with toggle button
- TypeScript compilation passes
- pnpm build completes without errors
</verification>

<success_criteria>
- Live timeline renders human-readable events in both workflow panels
- Run history panel accessible from TopBar
- Historical runs browsable with mode filtering
- Clicking a historical run opens its details
- All components follow existing UI patterns (Tailwind, lucide-react, framer-motion)
- Build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/13-agentic-side-panel-experience/13-02-SUMMARY.md`
</output>
